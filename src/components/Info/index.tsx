import React, { FunctionComponent } from 'react';

import { Container } from './styles';

const Info: FunctionComponent = () => {
  return (
    <Container>
      <h2>CSS: An Overview</h2>
      <select>
        <option value="25">25/26</option>
      </select>
    </Container>
  );
};

export default Info;
